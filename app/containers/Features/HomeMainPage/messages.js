/*
 * HomeMainPage Messages
 *
 * This contains all the text for the HomeMainPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.HomeMainPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeMainPage container!',
  },
});

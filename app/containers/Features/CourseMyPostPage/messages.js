/*
 * CourseMyPostPage Messages
 *
 * This contains all the text for the CourseMyPostPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CourseMyPostPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CourseMyPostPage container!',
  },
});

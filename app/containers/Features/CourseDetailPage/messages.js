/*
 * CourseDetailPage Messages
 *
 * This contains all the text for the CourseDetailPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CourseDetailPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CourseDetailPage container!',
  },
});

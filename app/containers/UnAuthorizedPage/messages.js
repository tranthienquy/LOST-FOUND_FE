/*
 * UnAuthorizedPage Messages
 *
 * This contains all the text for the UnAuthorizedPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UnAuthorizedPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UnAuthorizedPage container!',
  },
});

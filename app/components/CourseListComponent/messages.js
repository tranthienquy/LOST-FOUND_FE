/*
 * CourseListComponent Messages
 *
 * This contains all the text for the CourseListComponent component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CourseListComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CourseListComponent component!',
  },
});
